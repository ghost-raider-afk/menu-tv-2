const state = {
  authenticated: sessionStorage.getItem('menu-tv-2-authenticated') === 'true',
  page: 'dashboard',
  locations: [], screens: [], templates: [], sftpDirectories: [],
  sftpConnection: { host: '', port: 2022 },
  overview: { locations: 0, screens: 0, published: 0, templates: 0 }
};

const app = document.querySelector('#app');
const icons = {
  grid: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>',
  pin: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M20 10c0 5-8 11-8 11S4 15 4 10a8 8 0 1 1 16 0Z"/><circle cx="12" cy="10" r="2.6"/></svg>',
  screen: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="3" y="4" width="18" height="12" rx="2"/><path d="M8 20h8m-4-4v4"/></svg>',
  layers: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="m12 3 9 5-9 5-9-5 9-5Z"/><path d="m3 12 9 5 9-5M3 16l9 5 9-5"/></svg>',
  folder: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M3 7.5A2.5 2.5 0 0 1 5.5 5h4l2 2h7A2.5 2.5 0 0 1 21 9.5v8A2.5 2.5 0 0 1 18.5 20h-13A2.5 2.5 0 0 1 3 17.5v-10Z"/></svg>',
  key: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="8" cy="15" r="3"/><path d="m10.3 12.7 7.2-7.2 2 2-1.5 1.5 1.5 1.5-2.2 2.2-1.5-1.5-3.2 3.2"/></svg>',
  plus: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg>',
  upload: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M12 16V3m0 0 4 4m-4-4-4 4M4 15v4a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-4"/></svg>',
  send: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="m21 3-7.5 18-3.5-7-7-3.5L21 3Z"/><path d="m10 14 4-4"/></svg>',
  more: '<svg viewBox="0 0 24 24" fill="currentColor"><circle cx="5" cy="12" r="1.5"/><circle cx="12" cy="12" r="1.5"/><circle cx="19" cy="12" r="1.5"/></svg>',
  arrow: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="m9 18 6-6-6-6"/></svg>',
  check: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m5 12 4 4L19 6"/></svg>',
  exit: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M10 17l5-5-5-5M15 12H3m10 7v1a2 2 0 1 2-2 2H5a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h6a2 2 0 0 1 2 2v1"/></svg>'
};

function icon(name, classes = 'h-5 w-5') { return icons[name].replace('<svg ', `<svg class="${classes}" `); }
function escapeHtml(value) { return String(value ?? '').replace(/[&<>'"]/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[character]); }
async function api(path, options = {}) {
  const response = await fetch(path, { ...options, credentials: 'same-origin', headers: { ...(options.body ? { 'Content-Type': 'application/json' } : {}), ...options.headers } });
  if (response.status === 401) { state.authenticated = false; sessionStorage.removeItem('menu-tv-2-authenticated'); render(); throw new Error('Сессия истекла. Войдите снова.'); }
  if (!response.ok) { const body = await response.json().catch(() => ({})); throw new Error(body.error || 'Не удалось выполнить запрос.'); }
  return response.status === 204 ? null : response.json();
}
async function uploadJpeg(screenId, file) {
  const response = await fetch(`/api/screens/${screenId}/source`, { method: 'PUT', credentials: 'same-origin', headers: { 'Content-Type': 'image/jpeg' }, body: file });
  if (!response.ok) { const body = await response.json().catch(() => ({})); throw new Error(body.error || 'Не удалось загрузить JPEG.'); }
  return response.json();
}
async function refreshData() { [state.overview, state.locations, state.screens, state.templates, state.sftpDirectories, state.sftpConnection] = await Promise.all([api('/api/overview'), api('/api/locations'), api('/api/screens'), api('/api/templates'), api('/api/sftp/directories'), api('/api/sftp/connection')]); }

function loginView() { return `<section class="flex min-h-screen items-center justify-center bg-slate-950 px-5 py-10"><div class="w-full max-w-md rounded-3xl bg-white p-7 shadow-panel sm:p-10"><div class="mb-8 flex h-12 w-12 items-center justify-center rounded-2xl bg-indigo-600 text-white">${icon('screen', 'h-6 w-6')}</div><p class="text-sm font-semibold text-indigo-600">MENU TV 2.0</p><h1 class="mt-2 text-3xl font-bold tracking-tight text-slate-950">Независимая версия</h1><p class="mt-3 text-sm leading-6 text-slate-500">Отдельная база, SFTP-доступ и доставка меню на телевизоры.</p><form id="login-form" class="mt-8 space-y-5"><label class="block"><span class="field-label">Логин администратора</span><input class="field" name="username" autocomplete="username" required autofocus /></label><label class="block"><span class="field-label">Пароль администратора</span><input class="field" name="password" type="password" autocomplete="current-password" required /></label><p id="login-error" class="hidden text-sm font-medium text-rose-600"></p><button class="btn-primary w-full" type="submit">Открыть управление ${icon('arrow', 'h-4 w-4')}</button></form></div></section>`; }
function sidebarLink(page, label, glyph) { return `<button data-page="${page}" class="nav-link ${state.page === page ? 'is-active' : ''}">${icon(glyph, 'h-5 w-5')}<span>${label}</span></button>`; }
function shell(content) { return `<div class="min-h-screen lg:grid lg:grid-cols-[264px_minmax(0,1fr)]"><aside class="border-b border-slate-200 bg-white px-4 py-5 lg:min-h-screen lg:border-b-0 lg:border-r lg:px-5 lg:py-7"><div class="flex items-center gap-3 px-2"><span class="flex h-10 w-10 items-center justify-center rounded-xl bg-indigo-600 text-white">${icon('screen', 'h-5 w-5')}</span><span><strong class="block text-sm text-slate-950">Menu TV</strong><small class="text-xs font-medium text-slate-500">Версия 2.0</small></span></div><nav class="mt-8 space-y-1"><p class="px-3 pb-2 text-[11px] font-bold uppercase tracking-[0.14em] text-slate-400">Управление</p>${sidebarLink('dashboard', 'Обзор', 'grid')}${sidebarLink('locations', 'Точки', 'pin')}${sidebarLink('screens', 'Телевизоры', 'screen')}${sidebarLink('templates', 'Шаблоны', 'layers')}<p class="px-3 pt-5 pb-2 text-[11px] font-bold uppercase tracking-[0.14em] text-slate-400">Доставка</p>${sidebarLink('sftp', 'SFTP-доступ', 'folder')}</nav><div class="mt-8 rounded-2xl bg-slate-950 p-4 text-white"><p class="text-xs font-semibold uppercase tracking-[0.14em] text-slate-400">SFTP</p><p class="mt-2 text-sm font-semibold">Только чтение для ТВ</p><p class="mt-1 text-xs leading-5 text-slate-400">Один доступ на торговую точку, без прав изменения файлов.</p></div></aside><div class="min-w-0"><header class="flex h-20 items-center justify-between border-b border-slate-200 bg-white px-5 sm:px-8"><div><p class="text-xs font-medium text-slate-400">MENU TV 2.0</p><p class="mt-0.5 text-sm font-semibold text-slate-800">Панель управления</p></div><button data-action="logout" class="btn-secondary !px-3">${icon('exit', 'h-4 w-4')}<span class="hidden sm:inline">Выйти</span></button></header><main class="p-5 sm:p-8">${content}</main></div></div>`; }
function heading(title, description, action = '') { return `<div class="mb-7 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 class="text-2xl font-bold tracking-tight text-slate-950 sm:text-3xl">${title}</h1><p class="mt-2 text-sm text-slate-500">${description}</p></div>${action}</div>`; }
function statCard(label, value, kind) { return `<article class="card p-5"><div class="flex items-start justify-between"><span class="flex h-11 w-11 items-center justify-center rounded-xl bg-indigo-50 text-indigo-600">${icon(kind, 'h-5 w-5')}</span><span class="text-xs font-semibold text-emerald-600">${icon('check', 'mr-1 inline h-3.5 w-3.5')}готово</span></div><p class="mt-5 text-3xl font-bold tracking-tight text-slate-950">${value}</p><p class="mt-1 text-sm font-medium text-slate-500">${label}</p></article>`; }
function dashboardView() { return shell(`${heading('Обзор', 'Независимая среда Menu TV 2.0 и управляемая доставка на телевизоры.')}<section class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">${statCard('Точек', state.overview.locations, 'pin')}${statCard('Телевизоров', state.overview.screens, 'screen')}${statCard('Опубликовано', state.overview.published, 'check')}${statCard('Шаблонов', state.overview.templates, 'layers')}</section><section class="card mt-6 overflow-hidden"><div class="border-b border-slate-100 px-6 py-5"><h2 class="font-semibold text-slate-900">Порядок подключения телевизоров</h2><p class="mt-1 text-sm text-slate-500">Каталог создаётся только по явному действию администратора. Точка и название телевизора не меняют технический путь.</p></div><div class="grid divide-y divide-slate-100 md:grid-cols-3 md:divide-x md:divide-y-0"><button data-page="sftp" class="group p-6 text-left hover:bg-slate-50"><span class="text-indigo-600">${icon('folder', 'h-6 w-6')}</span><h3 class="mt-4 font-semibold text-slate-900">1. Подготовьте каталог</h3><p class="mt-1 text-sm leading-6 text-slate-500">Задайте имя и отдельно создайте его на SFTP-диске.</p></button><button data-page="sftp" class="group p-6 text-left hover:bg-slate-50"><span class="text-indigo-600">${icon('key', 'h-6 w-6')}</span><h3 class="mt-4 font-semibold text-slate-900">2. Привяжите точку</h3><p class="mt-1 text-sm leading-6 text-slate-500">Один логин и пароль с доступом только на чтение.</p></button><button data-page="screens" class="group p-6 text-left hover:bg-slate-50"><span class="text-indigo-600">${icon('upload', 'h-6 w-6')}</span><h3 class="mt-4 font-semibold text-slate-900">3. Опубликуйте JPEG</h3><p class="mt-1 text-sm leading-6 text-slate-500">Файл заменяется атомарно, поэтому ТВ не увидит незавершённую загрузку.</p></button></div></section>`); }
function emptyState(title, text, action) { return `<div class="px-6 py-16 text-center"><div class="mx-auto flex h-12 w-12 items-center justify-center rounded-2xl bg-indigo-50 text-indigo-600">${icon('plus', 'h-5 w-5')}</div><h3 class="mt-4 font-semibold text-slate-900">${title}</h3><p class="mx-auto mt-2 max-w-sm text-sm leading-6 text-slate-500">${text}</p><button data-action="${action}" class="btn-primary mt-5">Добавить</button></div>`; }
function statusPill(status) { return status === 'published' ? '<span class="pill-green">Опубликован</span>' : status === 'ready' ? '<span class="pill-indigo">JPEG готов</span>' : '<span class="pill-slate">Черновик</span>'; }
function rowMenu(type, id) { return `<div class="flex justify-end gap-2"><button data-action="edit-${type}" data-id="${id}" class="rounded-lg p-2 text-slate-400 hover:bg-slate-100 hover:text-slate-700" title="Изменить">${icon('more', 'h-5 w-5')}</button><button data-action="delete-${type}" data-id="${id}" class="rounded-lg p-2 text-slate-400 hover:bg-rose-50 hover:text-rose-600" title="Удалить">×</button></div>`; }

function listView(type) {
  const config = { locations: { title: 'Точки', description: 'Заведения и отдельные локации. SFTP-каталог не зависит от названия точки.', rows: state.locations, action: 'location', columns: ['Название', 'Адрес', 'Телевизоры', 'SFTP-каталог', 'Статус'] }, screens: { title: 'Телевизоры', description: 'Загрузите подготовленный JPEG и отдельно опубликуйте его в стабильный SFTP-путь.', rows: state.screens, action: 'screen', columns: ['Телевизор', 'Точка', 'SFTP-файл', 'Состояние', 'Публикация'] }, templates: { title: 'Шаблоны', description: 'Основа внешнего вида будущих меню.', rows: state.templates, action: 'template', columns: ['Название', 'Описание', 'Статус'] } }[type];
  const action = `<button data-action="new-${config.action}" class="btn-primary">${icon('plus', 'h-4 w-4')}Добавить</button>`;
  const rows = config.rows.map((row) => {
    if (type === 'locations') return `<tr><td><button data-action="edit-location" data-id="${row.id}" class="font-semibold text-slate-900 hover:text-indigo-600">${escapeHtml(row.name)}</button></td><td class="text-slate-500">${escapeHtml(row.address || '—')}</td><td class="text-slate-700">${row.screen_count}</td><td class="font-mono text-xs text-slate-600">${escapeHtml(row.sftp_directory_name ? `/${row.sftp_directory_name}` : 'не привязан')}</td><td>${row.active ? '<span class="pill-green">Активна</span>' : '<span class="pill-slate">Неактивна</span>'}</td><td>${rowMenu('location', row.id)}</td></tr>`;
    if (type === 'screens') return `<tr><td><button data-action="edit-screen" data-id="${row.id}" class="font-semibold text-slate-900 hover:text-indigo-600">${escapeHtml(row.name)}</button></td><td class="text-slate-500">${escapeHtml(row.location_name)}</td><td><span class="font-mono text-xs text-slate-600">${escapeHtml(row.sftp_path || 'привяжите каталог')}</span><p class="mt-1 text-xs text-slate-400">В клиенте: /${escapeHtml(row.delivery_filename)}</p></td><td>${statusPill(row.status)}</td><td><div class="flex items-center gap-1"><button data-action="upload-screen" data-id="${row.id}" class="btn-secondary !px-2 !py-1.5 text-xs">${icon('upload', 'h-3.5 w-3.5')}JPEG</button><button data-action="publish-screen" data-id="${row.id}" class="btn-secondary !px-2 !py-1.5 text-xs">${icon('send', 'h-3.5 w-3.5')}В эфир</button></div></td><td>${rowMenu('screen', row.id)}</td></tr>`;
    return `<tr><td><button data-action="edit-template" data-id="${row.id}" class="font-semibold text-slate-900 hover:text-indigo-600">${escapeHtml(row.name)}</button></td><td class="max-w-md text-slate-500">${escapeHtml(row.description || '—')}</td><td>${row.active ? '<span class="pill-green">Активен</span>' : '<span class="pill-slate">Неактивен</span>'}</td><td>${rowMenu('template', row.id)}</td></tr>`;
  }).join('');
  return shell(`${heading(config.title, config.description, action)}<section class="card overflow-hidden">${config.rows.length ? `<div class="overflow-x-auto"><table class="w-full min-w-[760px] text-left text-sm"><thead class="bg-slate-50 text-xs font-semibold uppercase tracking-wide text-slate-500"><tr>${config.columns.map((column) => `<th class="px-6 py-4">${column}</th>`).join('')}<th class="w-14 px-4 py-4"><span class="sr-only">Действия</span></th></tr></thead><tbody class="divide-y divide-slate-100">${rows}</tbody></table></div>` : emptyState(`Пока нет: ${config.title.toLowerCase()}`, 'Создайте первую запись в новой изолированной версии.', `new-${config.action}`)}</section>`);
}

function directoryState(directory) { return directory.storage_status === 'ready' ? '<span class="pill-green">На диске</span>' : '<span class="pill-slate">Не создан</span>'; }
function sftpView() {
  const directoryRows = state.sftpDirectories.map((directory) => `<tr><td><p class="font-mono text-sm font-semibold text-slate-900">/${escapeHtml(directory.name)}</p><p class="mt-1 text-xs text-slate-400">Имя пути неизменно после создания.</p></td><td>${directoryState(directory)}</td><td>${directory.bound_location_name ? `<span class="font-medium text-slate-700">${escapeHtml(directory.bound_location_name)}</span>` : '<span class="text-slate-400">Свободен</span>'}</td><td><div class="flex justify-end gap-2">${directory.storage_status === 'missing' ? `<button data-action="provision-directory" data-id="${directory.id}" class="btn-secondary !px-3 !py-2 text-xs">Создать на диске</button>` : ''}${!directory.bound_location_id ? `<button data-action="delete-directory" data-id="${directory.id}" class="rounded-lg px-2 text-rose-600 hover:bg-rose-50" title="Удалить запись">×</button>` : ''}</div></td></tr>`).join('');
  const pointRows = state.locations.map((location) => `<tr><td><p class="font-semibold text-slate-900">${escapeHtml(location.name)}</p><p class="mt-1 text-xs text-slate-400">${escapeHtml(location.address || 'Без адреса')}</p></td><td>${location.sftp_directory_name ? `<span class="font-mono text-xs text-slate-600">/${escapeHtml(location.sftp_directory_name)}</span>` : '<span class="text-slate-400">Не привязан</span>'}</td><td>${location.sftp_username ? `<span class="font-mono text-sm text-slate-700">${escapeHtml(location.sftp_username)}</span><p class="mt-1 text-xs text-slate-400">только чтение</p>` : '<span class="text-slate-400">—</span>'}</td><td><div class="flex justify-end gap-2">${location.sftp_username ? `<button data-action="reset-sftp-password" data-id="${location.id}" class="btn-secondary !px-3 !py-2 text-xs">Новый пароль</button><button data-action="unbind-sftp" data-id="${location.id}" class="rounded-lg px-2 text-rose-600 hover:bg-rose-50" title="Отключить доступ">×</button>` : `<button data-action="bind-sftp" data-id="${location.id}" class="btn-primary !px-3 !py-2 text-xs">Привязать</button>`}</div></td></tr>`).join('');
  return shell(`${heading('SFTP-доступ', 'Каталоги создаются и привязываются только по явному действию администратора.', `<button data-action="new-directory" class="btn-primary">${icon('plus', 'h-4 w-4')}Каталог</button>`)}<section class="card mb-6 p-5"><div class="flex flex-wrap items-center gap-x-8 gap-y-3"><div><p class="field-label">Сервер для CX Проводника</p><p class="mt-1 font-mono text-sm font-semibold text-slate-900">${escapeHtml(state.sftpConnection.host)}:${escapeHtml(state.sftpConnection.port)}</p></div><p class="max-w-xl text-sm leading-6 text-slate-500">На телевизоре укажите протокол SFTP, этот сервер, порт и данные своей точки. Все телевизоры точки используют одну учётную запись и могут только просматривать файлы.</p></div></section><section class="card overflow-hidden"><div class="border-b border-slate-100 px-6 py-5"><h2 class="font-semibold text-slate-900">SFTP-каталоги</h2><p class="mt-1 text-sm text-slate-500">Сначала создайте запись, затем отдельно подтвердите создание физического каталога.</p></div>${state.sftpDirectories.length ? `<div class="overflow-x-auto"><table class="w-full min-w-[700px] text-left text-sm"><thead class="bg-slate-50 text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-6 py-4">Каталог</th><th class="px-6 py-4">Состояние</th><th class="px-6 py-4">Точка</th><th class="px-4 py-4"></th></tr></thead><tbody class="divide-y divide-slate-100">${directoryRows}</tbody></table></div>` : emptyState('Каталогов пока нет', 'Создайте первый технический каталог и только затем привяжите его к точке.', 'new-directory')}</section><section class="card mt-6 overflow-hidden"><div class="border-b border-slate-100 px-6 py-5"><h2 class="font-semibold text-slate-900">Доступы торговых точек</h2><p class="mt-1 text-sm text-slate-500">Смена названия точки не меняет каталог или путь экранов.</p></div>${state.locations.length ? `<div class="overflow-x-auto"><table class="w-full min-w-[700px] text-left text-sm"><thead class="bg-slate-50 text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-6 py-4">Точка</th><th class="px-6 py-4">Каталог</th><th class="px-6 py-4">Логин</th><th class="px-4 py-4"></th></tr></thead><tbody class="divide-y divide-slate-100">${pointRows}</tbody></table></div>` : emptyState('Сначала создайте точку', 'У SFTP-доступа всегда есть конкретная торговая точка.', 'new-location')}</section>`);
}

function modal(title, body) { return `<div id="modal" class="fixed inset-0 z-20 flex items-end bg-slate-950/40 p-0 sm:items-center sm:justify-center sm:p-5"><div class="w-full rounded-t-3xl bg-white p-6 shadow-panel sm:max-w-lg sm:rounded-3xl"><div class="mb-6 flex items-center justify-between"><h2 class="text-lg font-bold text-slate-950">${title}</h2><button data-action="close-modal" class="rounded-lg px-2 py-1 text-2xl leading-none text-slate-400 hover:bg-slate-100">×</button></div>${body}</div></div>`; }
function formFor(type, record = {}) {
  if (type === 'location') return modal(record.id ? 'Изменить точку' : 'Новая точка', `<form data-form="location" data-id="${record.id || ''}" class="space-y-4"><label class="block"><span class="field-label">Название</span><input class="field" name="name" maxlength="120" required value="${escapeHtml(record.name || '')}" /></label><label class="block"><span class="field-label">Адрес</span><input class="field" name="address" maxlength="300" value="${escapeHtml(record.address || '')}" /></label><label class="flex items-center gap-3 text-sm font-medium text-slate-700"><input name="active" type="checkbox" ${record.active !== false ? 'checked' : ''} />Активна</label><div class="flex justify-end gap-3 pt-3"><button type="button" data-action="close-modal" class="btn-secondary">Отмена</button><button class="btn-primary">Сохранить</button></div></form>`);
  if (type === 'screen') { const options = state.locations.map((location) => `<option value="${location.id}" ${Number(record.location_id) === location.id ? 'selected' : ''}>${escapeHtml(location.name)}</option>`).join(''); return modal(record.id ? 'Изменить телевизор' : 'Новый телевизор', `<form data-form="screen" data-id="${record.id || ''}" class="space-y-4"><label class="block"><span class="field-label">Название</span><input class="field" name="name" maxlength="120" required value="${escapeHtml(record.name || '')}" /></label><label class="block"><span class="field-label">Точка</span><select class="field" name="location_id" required><option value="">Выберите точку</option>${options}</select></label><div class="grid gap-4 sm:grid-cols-2"><label class="block"><span class="field-label">Разрешение</span><input class="field" name="resolution" maxlength="32" value="${escapeHtml(record.resolution || '1920×1080')}" /></label><label class="block"><span class="field-label">Состояние</span><select class="field" name="status"><option value="draft" ${record.status === 'draft' ? 'selected' : ''}>Черновик</option><option value="ready" ${record.status === 'ready' ? 'selected' : ''}>JPEG готов</option></select></label></div><label class="flex items-center gap-3 text-sm font-medium text-slate-700"><input name="active" type="checkbox" ${record.active !== false ? 'checked' : ''} />Активен</label>${record.delivery_filename ? `<p class="rounded-xl bg-slate-50 px-3 py-2 font-mono text-xs text-slate-500">Имя файла: /${escapeHtml(record.delivery_filename)}</p>` : ''}<div class="flex justify-end gap-3 pt-3"><button type="button" data-action="close-modal" class="btn-secondary">Отмена</button><button class="btn-primary">Сохранить</button></div></form>`); }
  return modal(record.id ? 'Изменить шаблон' : 'Новый шаблон', `<form data-form="template" data-id="${record.id || ''}" class="space-y-4"><label class="block"><span class="field-label">Название</span><input class="field" name="name" maxlength="120" required value="${escapeHtml(record.name || '')}" /></label><label class="block"><span class="field-label">Описание</span><textarea class="field min-h-28" name="description" maxlength="500">${escapeHtml(record.description || '')}</textarea></label><label class="flex items-center gap-3 text-sm font-medium text-slate-700"><input name="active" type="checkbox" ${record.active !== false ? 'checked' : ''} />Активен</label><div class="flex justify-end gap-3 pt-3"><button type="button" data-action="close-modal" class="btn-secondary">Отмена</button><button class="btn-primary">Сохранить</button></div></form>`);
}
function directoryForm() { return modal('Новый SFTP-каталог', `<form data-form="sftp-directory" class="space-y-4"><p class="rounded-xl bg-amber-50 px-3 py-3 text-sm leading-5 text-amber-800">Это только регистрация имени. Физический каталог появится после отдельного подтверждения в списке.</p><label class="block"><span class="field-label">Имя каталога</span><input class="field font-mono" name="name" maxlength="64" pattern="[A-Za-z0-9][A-Za-z0-9._-]{0,63}" placeholder="store-01" required autofocus /><span class="mt-1 block text-xs text-slate-400">Латинские буквы, цифры, точка, дефис и подчёркивание.</span></label><div class="flex justify-end gap-3 pt-3"><button type="button" data-action="close-modal" class="btn-secondary">Отмена</button><button class="btn-primary">Зарегистрировать</button></div></form>`); }
function bindingForm(location) { const options = state.sftpDirectories.filter((directory) => !directory.bound_location_id && directory.storage_status === 'ready').map((directory) => `<option value="${directory.id}">/${escapeHtml(directory.name)}</option>`).join(''); return modal(`SFTP-доступ: ${escapeHtml(location.name)}`, options ? `<form data-form="sftp-binding" data-id="${location.id}" class="space-y-4"><p class="text-sm leading-6 text-slate-500">Логин и пароль будут общими для всех телевизоров этой точки. Пароль покажется только один раз.</p><label class="block"><span class="field-label">Каталог</span><select class="field" name="directory_id" required>${options}</select></label><label class="block"><span class="field-label">Логин SFTP</span><input class="field font-mono" name="username" maxlength="32" pattern="[A-Za-z][A-Za-z0-9_-]{2,31}" placeholder="store_01" required autofocus /></label><div class="flex justify-end gap-3 pt-3"><button type="button" data-action="close-modal" class="btn-secondary">Отмена</button><button class="btn-primary">Создать доступ</button></div></form>` : `<p class="text-sm leading-6 text-slate-600">Нет свободного созданного каталога. Сначала добавьте и явно создайте каталог в верхнем блоке.</p>`); }
function screenMediaForm(screen) { return modal(`JPEG: ${escapeHtml(screen.name)}`, `<form data-form="screen-media" data-id="${screen.id}" class="space-y-4"><p class="rounded-xl bg-slate-50 px-3 py-3 text-sm leading-6 text-slate-600">Подготовленный JPEG сохраняется отдельно. Текущий файл на телевизоре заменится только после нажатия «В эфир».</p><label class="block"><span class="field-label">Подготовленный JPEG</span><input class="field !p-2" name="jpeg" type="file" accept="image/jpeg" required /></label><div class="flex justify-end gap-3 pt-3"><button type="button" data-action="close-modal" class="btn-secondary">Отмена</button><button class="btn-primary">Загрузить JPEG</button></div></form>`); }
function credentialsModal(credentials) { return modal('Сохраните SFTP-доступ', `<p class="mb-5 rounded-xl bg-amber-50 px-3 py-3 text-sm leading-6 text-amber-800">Пароль больше не будет показан. Для нового пароля используйте действие «Новый пароль» у точки.</p><dl class="space-y-3 rounded-2xl bg-slate-950 p-5 font-mono text-sm text-white"><div><dt class="text-xs uppercase tracking-wide text-slate-400">Сервер</dt><dd class="mt-1 select-all">${escapeHtml(credentials.host)}</dd></div><div><dt class="text-xs uppercase tracking-wide text-slate-400">Порт</dt><dd class="mt-1 select-all">${escapeHtml(credentials.port)}</dd></div><div><dt class="text-xs uppercase tracking-wide text-slate-400">Логин</dt><dd class="mt-1 select-all">${escapeHtml(credentials.username)}</dd></div><div><dt class="text-xs uppercase tracking-wide text-slate-400">Пароль</dt><dd class="mt-1 select-all">${escapeHtml(credentials.password)}</dd></div></dl><div class="mt-5 flex justify-end"><button data-action="close-modal" class="btn-primary">Сохранил</button></div>`); }
function notice(message, type = 'success') { const colour = type === 'error' ? 'bg-rose-600' : 'bg-slate-950'; document.querySelector('#notice')?.remove(); document.body.insertAdjacentHTML('beforeend', `<div id="notice" class="fixed bottom-5 left-1/2 z-30 -translate-x-1/2 rounded-xl ${colour} px-4 py-3 text-sm font-medium text-white shadow-lg">${escapeHtml(message)}</div>`); window.setTimeout(() => document.querySelector('#notice')?.remove(), 3500); }
function render() { if (!state.authenticated) { app.innerHTML = loginView(); return; } app.innerHTML = state.page === 'dashboard' ? dashboardView() : state.page === 'sftp' ? sftpView() : listView(state.page); }
async function loadAndRender() { try { await refreshData(); render(); } catch (error) { if (state.authenticated) notice(error.message, 'error'); } }
function openModal(markup) { document.querySelector('#modal')?.remove(); document.body.insertAdjacentHTML('beforeend', markup); }

document.addEventListener('click', async (event) => {
  const target = event.target.closest('[data-action], [data-page]'); if (!target) return;
  if (target.dataset.page) { state.page = target.dataset.page; render(); return; }
  const action = target.dataset.action;
  if (action === 'logout') { try { await api('/api/auth/logout', { method: 'POST' }); } catch { /* Session may already be expired. */ } state.authenticated = false; sessionStorage.removeItem('menu-tv-2-authenticated'); render(); return; }
  if (action === 'close-modal') return document.querySelector('#modal')?.remove();
  if (action === 'new-directory') return openModal(directoryForm());
  if (action === 'provision-directory') { const directory = state.sftpDirectories.find((item) => item.id === Number(target.dataset.id)); if (!directory || !window.confirm(`Создать физический каталог /${directory.name} на SFTP-диске?`)) return; try { await api(`/api/sftp/directories/${directory.id}/provision`, { method: 'POST' }); await loadAndRender(); notice('Каталог создан на SFTP-диске.'); } catch (error) { notice(error.message, 'error'); } return; }
  if (action === 'delete-directory') { const directory = state.sftpDirectories.find((item) => item.id === Number(target.dataset.id)); if (!directory || !window.confirm(`Удалить только запись каталога /${directory.name}? Физический каталог и файлы не будут удалены.`)) return; try { await api(`/api/sftp/directories/${directory.id}`, { method: 'DELETE' }); await loadAndRender(); notice('Запись каталога удалена.'); } catch (error) { notice(error.message, 'error'); } return; }
  if (action === 'bind-sftp') { const location = state.locations.find((item) => item.id === Number(target.dataset.id)); if (location) openModal(bindingForm(location)); return; }
  if (action === 'reset-sftp-password') { const location = state.locations.find((item) => item.id === Number(target.dataset.id)); if (!location || !window.confirm(`Сменить SFTP-пароль точки «${location.name}»? Телевизоры потребуется обновить новым паролем.`)) return; try { const result = await api(`/api/locations/${location.id}/sftp-password`, { method: 'POST' }); await loadAndRender(); openModal(credentialsModal(result.credentials)); } catch (error) { notice(error.message, 'error'); } return; }
  if (action === 'unbind-sftp') { const location = state.locations.find((item) => item.id === Number(target.dataset.id)); if (!location || !window.confirm(`Отключить SFTP-доступ точки «${location.name}»? Каталог и опубликованные файлы останутся на диске.`)) return; try { await api(`/api/locations/${location.id}/sftp-binding`, { method: 'DELETE' }); await loadAndRender(); notice('SFTP-доступ отключён.'); } catch (error) { notice(error.message, 'error'); } return; }
  if (action === 'upload-screen') { const screen = state.screens.find((item) => item.id === Number(target.dataset.id)); if (screen) openModal(screenMediaForm(screen)); return; }
  if (action === 'publish-screen') { const screen = state.screens.find((item) => item.id === Number(target.dataset.id)); if (!screen || !window.confirm(`Опубликовать JPEG в ${screen.sftp_path || 'SFTP-каталог'}?`)) return; try { await api(`/api/screens/${screen.id}/publish`, { method: 'POST' }); await loadAndRender(); notice('JPEG опубликован.'); } catch (error) { notice(error.message, 'error'); } return; }
  const [verb, type] = action.split('-');
  if (verb === 'new') { if (type === 'screen' && state.locations.length === 0) return notice('Сначала добавьте точку.', 'error'); openModal(formFor(type)); return; }
  const collection = `${type}s`; const record = state[collection]?.find((item) => item.id === Number(target.dataset.id));
  if (verb === 'edit' && record) openModal(formFor(type, record));
  if (verb === 'delete' && record && window.confirm(`Удалить «${record.name}»?`)) { try { await api(`/api/${collection}/${record.id}`, { method: 'DELETE' }); await loadAndRender(); notice('Удалено.'); } catch (error) { notice(error.message, 'error'); } }
});

document.addEventListener('submit', async (event) => {
  if (event.target.id === 'login-form') { event.preventDefault(); const form = new FormData(event.target); try { await api('/api/auth/login', { method: 'POST', body: JSON.stringify({ username: form.get('username'), password: form.get('password') }) }); state.authenticated = true; sessionStorage.setItem('menu-tv-2-authenticated', 'true'); await loadAndRender(); } catch (error) { document.querySelector('#login-error').textContent = error.message; document.querySelector('#login-error').classList.remove('hidden'); } return; }
  const type = event.target.dataset.form; if (!type) return; event.preventDefault(); const form = new FormData(event.target); const id = event.target.dataset.id;
  try {
    if (type === 'sftp-directory') { await api('/api/sftp/directories', { method: 'POST', body: JSON.stringify({ name: form.get('name') }) }); document.querySelector('#modal')?.remove(); await loadAndRender(); notice('Имя каталога зарегистрировано. Теперь создайте его на диске.'); return; }
    if (type === 'sftp-binding') { const result = await api(`/api/locations/${id}/sftp-binding`, { method: 'POST', body: JSON.stringify({ directory_id: Number(form.get('directory_id')), username: form.get('username') }) }); document.querySelector('#modal')?.remove(); await loadAndRender(); openModal(credentialsModal(result.credentials)); return; }
    if (type === 'screen-media') { const file = form.get('jpeg'); if (!(file instanceof File) || file.size === 0) throw new Error('Выберите JPEG-файл.'); await uploadJpeg(id, file); document.querySelector('#modal')?.remove(); await loadAndRender(); notice('JPEG подготовлен. Теперь опубликуйте его в эфир.'); return; }
    const data = Object.fromEntries(form.entries()); data.active = form.get('active') === 'on'; if (type === 'screen') data.location_id = Number(data.location_id);
    await api(`/api/${type}s${id ? `/${id}` : ''}`, { method: id ? 'PUT' : 'POST', body: JSON.stringify(data) }); document.querySelector('#modal')?.remove(); await loadAndRender(); notice('Сохранено.');
  } catch (error) { notice(error.message, 'error'); }
});

if (state.authenticated) loadAndRender(); else render();
