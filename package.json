{
  "name": "menu-tv-2",
  "version": "0.1.0",
  "private": true,
  "description": "Independent Menu TV 2.0 administration application",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "node --check src/web/admin-ui/public/app.js",
    "start": "node src/server.js",
    "test": "node --test",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5"
  }
}
